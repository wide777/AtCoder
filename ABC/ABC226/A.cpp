#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define REP(i, n) for(int i = 0; i < (int)(n); i++)

int main() {
    double X;
    cin >> X;
    cout << round(X) << endl;

    return 0;
}