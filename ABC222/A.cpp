#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define REP(i, n) for(int i = 0; i < (int)(n); i++)

int main() {
    string N;
    cin >> N;
    cout << setfill('0') << right << setw(4);
    cout << N << endl;

    return 0;
}