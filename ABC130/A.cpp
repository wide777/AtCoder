#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define REP(i, n) for(int i = 0; i < (int)(n); i++)

int main() {
    int x, a;
    cin >> x >> a;
    if(x < a) {
        cout << 0 << endl;
    } else {
        cout << 10 << endl;
    }
}